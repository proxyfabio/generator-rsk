"use_strict";

var React = require('react');
var RouteActions = require('../actions/<%= name %>Actions');

var <%= name %> = React.createClass({

  render() {
    return (

    );
  }

});

module.exports = <%= name %>;
